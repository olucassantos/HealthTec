import React from "react";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    legendaCampo: {
        fontWeight: "bold",
        fontSize: 16,
        color: "#fff"
    },
    campoTexto: {
        borderWidth: 1,
        borderColor: "#fff",
        padding: 10,
        marginBottom: 15,
        borderRadius: 3,
        color: "#fff"
    },
    planoFundo: {
        backgroundColor: "#ccc",
        width: "100%",
        paddingTop: 20,
    },
    fundoFormulario: {
        backgroundColor: "#2e5bf2",
        width: "70%",
        margin: "auto",
        borderTopStartRadius: 10,
        borderTopEndRadius: 10,
        padding: 10,
    }
});

export default styles;